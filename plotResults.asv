function p = plotResults(results)
    c = categorical({'unrelated', 'agree', 'disagree', 'discuss'});
    d = zeros(4, 2);
    for i = 1:length(results)
        
        if strcmp(results.original(i), results.result(i))
            
        end
    p = 2;
end
    